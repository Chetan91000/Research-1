# Project-Paper Mismatch Analysis

This document identifies the critical alignment gaps and divergences between the theoretical research draft ("Fully Local AI-powered Interactive Application Security Lab") and the actual implemented codebase (**Sentinel Desktop** in `Major Project/`).

---

## Mismatch & Alignment Matrix

| Paper Claim | Actual Implementation | Status | Evidence in Codebase | Action Needed in Paper |
| :--- | :--- | :--- | :--- | :--- |
| **Autonomous Attack Planning & Exploitation** | The codebase is **strictly defensive and non-destructive**. It explicitly bans exploit payload generation and focuses on passive inspection, inert canary reflection, and remediation advice. | **DIVERGENT (Core Orientation)** | `Major Project/README.md:20`<br>`src/advisor/ollama.ts:78` (*"Do NOT generate exploit payloads, attack strings, or instructions on how to attack"*). | **CRITICAL UPDATE:** Realign the paper's narrative from offensive autonomous penetration testing to an **evidence-first, interactive application security lab for defensive inspection, inert verification, and grounded AI remediation**. |
| **Local LLM Integration** | Implemented via local Ollama API (`127.0.0.1:11434`) using `llama3:latest` for grounded finding explanation and multi-framework code remediation. | **IMPLEMENTED** | `src/advisor/ollama.ts:61-157`<br>`electron/main.cjs:502-564` | Detail the exact prompt architecture, schema enforcement (JSON), temperature (0.2), and loopback isolation in Section IV-B and Section V. |
| **Interactive Browser Environment** | Implemented as an Electron `WebContentsView` with CDP debugger attachment (`Runtime`, `Network`), live request interception (`webRequest`), and origin locking. | **IMPLEMENTED** | `electron/main.cjs:217-380` | Update Section IV-C and Section V with exact technical specifications of the Electron/CDP instrumentation layer. |
| **Vulnerability Verification** | Implemented via a deterministic **Replay Comparator** (`compareReplayEvidence`) that compares baseline vs. re-test evidence, calculates header diffs, and evaluates canary persistence. | **IMPLEMENTED (Novel Concrete Mechanism)** | `src/engine/replay.ts:57-177` | Feature the **Replay & Header Diff Engine** prominently in Section IV-F as the concrete solution to Challenge #4 (Vulnerability Verification). |
| **Active Reflection / XSS Probing** | Implemented via **Inert Canary Probing** (`canary.ts`) analyzing reflection across 5 DOM/header contexts without malicious payloads. | **IMPLEMENTED (Unstated in Paper)** | `src/engine/canary.ts:4-131` | Add the **Inert Canary Engine** to Section IV-D/F to explain how active testing is performed safely without hallucinating dangerous exploit strings. |
| **Evidence Collection & Reproducibility** | Implemented with unique event IDs, ISO timestamps, SHA-256 integrity hashes, sensitive data/header redaction, and `.kilo/archives` rotation. | **IMPLEMENTED (Unstated in Paper)** | `electron/main.cjs:79-169`<br>`evidence.json` | Incorporate the structured evidence schema, redaction pipeline, and hashing mechanisms into Section IV-G and Section V. |
| **Multi-Agent Swarm Orchestration** | Implemented as a unified, state-machine-driven desktop workflow (Consent -> Origin Lock -> Recording -> Canary -> Replay -> Advisor). | **MODIFIED** | `src/App.tsx`<br>`src/engine/stateMachine.ts` | Describe the architecture as an integrated modular pipeline rather than claiming an uncoordinated multi-agent swarm. |
| **External Scanner Ingestion** | Implemented native parsers for SARIF v2.1.0 (Nuclei, CodeQL) and OWASP ZAP JSON reports. | **IMPLEMENTED (Unstated in Paper)** | `src/engine/ingest.ts:99-239` | Include multi-source scanner ingestion in Section IV-D as part of the security analysis capabilities. |
| **RAG / Knowledge Retrieval** | Implemented via curated, rule-based CWE/OWASP knowledge injection (`getOfflineKnowledge`) coupled with citation grounding validation (`isGrounded`). | **PARTIALLY IMPLEMENTED** | `src/advisor/ollama.ts:23-59, 133` | Accurately describe the knowledge mechanism as a curated offline security ontology with evidence grounding verification rather than vector embeddings. |
| **Experimental Results & Metrics** | Test suite (`tests/engine.test.ts`), local fixture server (`fixtureServer.cjs`), and recorded session data (`evidence.json`) are present. Quantitative benchmarks against synthetic targets remain to be formally tabulated. | **INFRASTRUCTURE PRESENT / DATA PENDING** | `tests/engine.test.ts`<br>`evidence.json` | Use the test fixtures and `evidence.json` to define concrete empirical evaluation experiments in Section VI, keeping results strictly tied to measurable code capabilities. |
